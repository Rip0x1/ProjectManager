<Window x:Class="ProjectManagerApp.Views.ProjectCommentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ProjectManagementSystem.WPF.Converters"
        Title="Комментарии к проекту" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    
    <Border Background="{DynamicResource MaterialDesignPaper}"
            CornerRadius="8"
            Effect="{StaticResource MaterialDesignElevationShadow4}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" 
                    CornerRadius="8,8,0,0" Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <materialDesign:PackIcon Kind="CommentMultiple" Width="24" Height="24" 
                                               Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Комментарии к проекту" 
                                   Foreground="{DynamicResource MaterialDesignBody}"
                                   VerticalAlignment="Center"
                                   FontSize="18"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"
                            Margin="0,0,16,0"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Width="32" Height="32"
                            FontSize="16"
                            Click="Close_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource MaterialDesignDivider}" 
                        CornerRadius="4" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="{Binding ProjectName}" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   FontWeight="Bold"/>
                        <TextBlock Text="Добавьте комментарий или просмотрите существующие" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                    <ItemsControl ItemsSource="{Binding Comments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource MaterialDesignCardBackground}" 
                                        CornerRadius="8" 
                                        Margin="0,0,0,12"
                                        Padding="16"
                                        Effect="{StaticResource MaterialDesignElevationShadow2}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                            <materialDesign:PackIcon Kind="Account" Width="16" Height="16" 
                                                                   Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding AuthorName}" 
                                                       Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text=" • " 
                                                       Foreground="{DynamicResource MaterialDesignBodyLight}" 
                                                       VerticalAlignment="Center" Margin="4,0"/>
                                            <TextBlock Text="{Binding FormattedCreatedAt}" 
                                                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        </StackPanel>

                                        <TextBlock Grid.Row="1" Text="{Binding Content}" 
                                                   Style="{StaticResource MaterialDesignBody1TextBlock}"
                                                   Foreground="{DynamicResource MaterialDesignBody}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,8"/>

                                        <Button Grid.Row="2" 
                                                Content="Удалить" 
                                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                                HorizontalAlignment="Right"
                                                Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisibilityConverter}}"
                                                Command="{Binding DataContext.DeleteCommentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <Border Grid.Row="2" Background="{DynamicResource MaterialDesignDivider}" 
                        CornerRadius="4" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Добавить новый комментарий" 
                                   Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   FontWeight="SemiBold" Margin="0,0,0,8"/>
                        
                        <TextBox Text="{Binding CommentText, UpdateSourceTrigger=PropertyChanged}"
                                 materialDesign:HintAssist.Hint="Введите ваш комментарий..."
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 Height="80"
                                 VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="3" Background="{DynamicResource MaterialDesignDivider}" 
                    CornerRadius="0,0,8,8" Height="60">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <materialDesign:PackIcon Kind="Information" Width="16" Height="16" 
                                               Foreground="{DynamicResource MaterialDesignBodyLight}" 
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Comments.Count, StringFormat='Всего комментариев: {0}'}" 
                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" 
                            Content="Отмена" 
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="8,0,8,0"
                            Click="Cancel_Click"/>
                    <Button Grid.Column="2" 
                            Content="Добавить комментарий" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="8,0,16,0"
                            Command="{Binding AddCommentCommand}"
                            IsEnabled="{Binding CanAddComment}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
