<Window x:Class="ProjectManagementSystem.WPF.Views.CreateEditProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Foreground" Value="#37474F"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" Effect="{StaticResource MaterialDesignShadowDepth5}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="15,15,0,0" Padding="25,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{Binding WindowTitle}" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="White"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Click="Close_Click"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Width="32" Height="32"
                            ToolTip="Закрыть">
                        <materialDesign:PackIcon Kind="Close" Foreground="White" Width="20" Height="20"/>
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                <StackPanel>
                    <TextBlock Text="Название проекта *" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Введите название проекта"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="Описание проекта" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding ProjectDescription, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Введите описание проекта"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             FontSize="14"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="120"
                             MaxHeight="200"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="Руководитель проекта *" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Managers}"
                              SelectedItem="{Binding SelectedManager}"
                              DisplayMemberPath="FullName"
                              materialDesign:HintAssist.Hint="Выберите руководителя"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              FontSize="14"
                              Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Крайний срок" Style="{StaticResource LabelStyle}"/>
                            <DatePicker SelectedDate="{Binding Deadline}"
                                        materialDesign:HintAssist.Hint="Выберите дату"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                        FontSize="14"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Статус проекта" Style="{StaticResource LabelStyle}"/>
                            <ComboBox ItemsSource="{Binding ProjectStatuses}"
                                      SelectedItem="{Binding SelectedStatus}"
                                      DisplayMemberPath="Value"
                                      SelectedValuePath="Key"
                                      materialDesign:HintAssist.Hint="Выберите статус"
                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                      FontSize="14"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="* - обязательные поля" 
                               FontSize="12" 
                               FontStyle="Italic" 
                               Foreground="#757575"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" 
                    Background="#F5F5F5" 
                    CornerRadius="0,0,15,15" 
                    Padding="25,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ProgressBar Grid.Column="0"
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 VerticalAlignment="Center"
                                 Margin="0,0,20,0"/>

                    <Button Grid.Column="1"
                            Content="Отмена"
                            Click="Cancel_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Width="120"
                            Height="40"
                            Margin="0,0,15,0"
                            IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"/>

                    <Button Grid.Column="2"
                            Content="{Binding SaveButtonText}"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{StaticResource PrimaryBrush}"
                            BorderBrush="{StaticResource PrimaryBrush}"
                            Width="120"
                            Height="40"
                            IsEnabled="{Binding CanSave}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>

